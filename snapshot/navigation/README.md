# Snapshot Navigation

Record the custom Booking Engine menu, role visibility, Agent Workspace links, Knowledge Center entry, and administrator-only pages.

The navigation must lead employees from role → action → instruction without requiring knowledge of configuration internals.
