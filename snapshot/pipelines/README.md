# Snapshot Pipelines

The Snapshot pipeline maps client-specific milestones to Campaign → Capture → Convert → Close.

## Rules

- Use stages for customer state, not employee reminders.
- Permit one or multiple Convert milestones.
- Mark milestones required, conditional, or skippable.
- Keep commercial terminal status separate from appointment attendance.
- Record the pipeline and stage identifiers after Snapshot installation.
