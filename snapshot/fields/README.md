# Snapshot Fields

Use native Salesflow fields wherever they accurately represent the required state. Add custom fields only when the Booking Engine requires information that no native field owns.

- [Native field map](native.md)
- [Custom field contract](custom.md)

Every field entry must define object, type, allowed values, writer, readers, workflow dependencies, reporting use, and migration behavior.
